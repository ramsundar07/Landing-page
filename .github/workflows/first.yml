name: first
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
       - name: first step
         uses: actions/checkout@v2
       - name: second step
         run: ls -a
  second-job:
    runs-on: windows-latest
    steps:
       - name: first step
         uses: actions/checkout@v2
       - name: second step
         run: dir      
